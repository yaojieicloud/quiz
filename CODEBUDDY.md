# Quiz 项目协作军规

> 本文档是 LLM协助开发 Quiz 题库系统的最高协作准则。每次启动新会话时，LLM 必须先完整阅读本文件，严格遵守以下流程。

---

## 一、项目概况

- **项目名称**: Quiz 题库系统 (FastAPI + SQLite)
- **项目根目录**: `C:\Users\Yaojie\Documents\GitHub\quiz`
- **文档总入口**: `README.md` —— LLM 必须在阅读本文件后，按导航索引继续阅读相关设计文档。
- **目标用户**: 小学生/初中生（学员端），家长（家长端），管理员（后台）

## 二、启动必读 (每次新会话必做)

LLM 接收任务后，按以下顺序建立上下文：

1. **阅读本文档** (`CODEBUDDY.md`) —— 了解协作流程
2. **阅读** `README.md` —— 项目全貌、功能特性、快速开始
3. **按需阅读**设计文档 (位于 `docs/design/`)：
   - `系统架构总览.md` —— 技术栈、模块划分、部署方式
   - `数据库迁移机制.md` —— 加字段/改表的规范流程
   - 其他相关文档按需加载（LLM 应根据任务类型判断需要哪些）
4. **检查变更日志** `docs/changelog/CHANGELOG-YYYY-MM-DD.md`（如存在当天记录）

---

## 三、通用铁律 (不可违背)

### 3.1 教材铁律
所有题目内容必须严格基于 **人教版新课标课本（2024秋季改版）**。不得凭旧版教材或臆造出题。不确定的知识点，必须先搜索验证再写入。

### 3.2 文档先行铁律
任何改动必须有文档记录。**先写文档，后改代码。** 禁止口头描述代替文档。

### 3.3 主文档同步铁律
每次功能变更完成后，必须更新 `README.md` 中的相关章节（功能列表、目录结构等），确保文档总入口始终是最新状态。各设计文档也须同步维护。

### 3.4 数据源铁律
**所有数据查询以 ECS 数据库为准，严禁使用本地 data/quiz.db。**
- 查学员做题记录、精通度、统计数据 → 必须通过 ECS API（`/api/admin/exec-sql`）或下载 ECS 备份
- 本地 data/quiz.db 仅用于代码开发调试，不用于任何数据确认
- 理由：本地库与 ECS 不同步，用本地数据会得出错误结论

### 3.5 安全红线
- 禁止在代码中硬编码密钥/密码，统一使用 `.env`
- 涉及数据库批量操作前必须确认备份策略
- 推送 ECS 前必须在本地完整测试

### 3.6 开发质量保证

编码和推送 ECS 之前，必须严格遵守 `it-workflow` skill 中的**流程七：开发质量保证**（阶段 A→B→C→D）。核心要求：

- **改前**：读相关代码、确认数据源（ECS）、列出影响范围
- **编码**：遵守前端 JS 红线和后端 Python 红线
- **本地验证**：语法检查 + 逻辑验证 + 影响范围 + 边界场景（4 步）
- **ECS 验证**：P0 冒烟 → P1 功能 → P2 数据（分层验证）
- **验证报告**：每个需求/Bug 修复必须附验证报告模板

### 3.7 代码质量
- FastAPI 路由按模块拆分，不堆积在单个文件
- SQLAlchemy 模型变更走迁移流程（参考 `docs/design/数据库迁移机制.md`）
- 前端原生 HTML/CSS/JS，不引入不必要的框架依赖
- Python 代码遵循 PEP8，加必要的注释

---

## 四、需求提交流程

### 4.1 用户提交需求
用户在聊天中描述需求即可，无需固定格式。LLM应主动引导完善。

### 4.2 LLM 需求梳理 (必须)
收到需求后，LLM 执行以下步骤：

1. **理解确认**: 用自己的话复述需求，确认理解正确
2. **澄清提问**: 对模糊点、边界条件逐一提问，直到无歧义
3. **风险提醒**: 指出可能的技术难点、性能影响、兼容性风险
4. **产出文档**: 编写 `docs/requirements/[需求简述].md`，包含：
   - 需求编号 (自动递增)
   - 需求描述（用户原始描述）
   - 需求分析（LLM 自己的理解与拆解）
   - 功能列表（逐条列出，每条标注优先级 P0/P1/P2）
   - 边界条件与约束
   - 验收标准（可量化的测试用例）
5. **等待确认**: 将文档展示给用户，明确等待确认后才能进入下一步

### 4.3 用户确认需求
用户对需求文档审核后：
- 通过 → 明确回复"确认"，LLM 进入设计阶段
- 修改 → LLM 按反馈修订文档，再次确认
