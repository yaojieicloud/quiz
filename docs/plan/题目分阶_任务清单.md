# 题目分阶（难度档位）任务清单

> 设计文档：[题目分阶与难度档位设计](../design/题目分阶与难度档位设计.md)
> 规则：按优先级从 T1 往下做，**做完一项、回来把状态改成 ✅ 并补验收记录**，再开下一项。状态：⬜待办 / 🔄进行中 / ✅完成。

## P1 数据地基（一切的前置，最先做）

| ID | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| T1 | 迁移：建 `tier_config`+种子三档；`questions.tier`、`exam_records.tier` 加列并存量回填=1 | `src/migrations/0003_tier.py` | ✅ `schema_migrations` 含本版本；`tier_config` 三行；`questions.tier` 全 1（容器内临时库验证） |
| T2 | 常量层 `core/tier.py`（TIER 枚举/名称/倍率读取）+ `models.py` 加 `tier` 列声明 | `src/core/tier.py`、`src/models.py` | ✅ `get_tier_multiplier` 读表回退常量正确（改倍率后读取立即生效 2→5） |
| T3 | schemas 贯通 tier：`QuestionCreate/Update/Out/QuestionForExam`、`ExamStartRequest`、`ExamSubmitRequest`、`ExamRecordOut` | `src/schemas.py` | ✅ 序列化含 tier |

## P2 组卷与积分（核心体验）

| ID | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| T4 | `start_exam` 三模式按 `tier` 抽题；wrong 模式按 tier 过滤错题，无则空提示 | `src/routers/exam.py` | ✅ 抽题 query 加 `Question.tier==data.tier`；tier=2 命中0、tier=1 命中全量（Base.metadata 验证） |
| T5 | `submit_exam` 写 `record.tier`；`_award_exam_points` 基础分×倍率 | `src/routers/exam.py` | ✅ 实测 tier1=5/tier2=10/tier3=15（满分基础分×倍率），ALL PASS |
| T6 | `questions.py` create/batch 写入 `tier`（默认1） | `src/routers/questions.py` | ✅ Question(tier=...) 贯通 create/batch |

## P3 掌握度 tier 化

| ID | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| T7 | `core/mastery.py` 作用域改 `(uid,topic,tier)`：`_topic_totals`/`_load_student_rows`/`_rows_to_sessions` 带 tier；`_eval_topic` 不变 | `src/core/mastery.py` | ✅ 单测：初级精通、进阶未开始，档位独立 |
| T8 | `mastery.py` 路由输出按 tier 嵌套（me/student/class）；支持 `?tier=` | `src/routers/mastery.py` | ✅ `/mastery/me` 返回 `tiers`；class 矩阵 `cells[sid][tier]`（容器内 ORM 种子验证） |
| T9 | 前端掌握度加 tier 选择/展示 | `src/static/mastery.html`、`admin-mastery.html` | ✅ 学员端 master.html 加「难度档位」chip 切换，topicCard 改读 `t.tiers[selectedTier]`；admin-mastery 加档位下拉，cells 改读 `cells[sid][tier]`，深链跳转带 tier；node --check 通过 |

## P4 学情分析 tier 化

| ID | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| T10 | `analytics.py`：`_student_mastery_map` 按 tier；`weakness`/`overview`/`student` 支持 `tier` 过滤 | `src/routers/analytics.py` | ✅ 容器内直调三接口验证：tier=1/2 的 weak_topics(2主题 vs 1主题)、repeat_wrong(Q2 仅 tier2)、student by_topic/score_trend、overview by_subject(答案数4 vs 2) 全部按档位正确分流，tier_name 正确；markWeakTopics 深链带 tier |
| T11 | `admin.html` 学情 tab 加 tier 筛选与展示；双向跳转带 tier | `src/static/admin.html` | ✅ 总览/学员档案/薄弱分析三 pane 均加「难度档位」下拉并把 tier 传给后端；薄弱分析 hint 显示当前档位、跳转掌握度带 `&tier=`；node --check 通过 |

## P5 前端组卷与记录展示

| ID | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| T12 | `home.html` 组卷加 tier 单选 chip（多选一），`startExam` 传 tier | `src/static/home.html` | ✅ 组卷/实操弹层均加「难度档位」单选 chip（初级默认），startExam/startCourseExam 请求带 `tier` 并写入 localStorage；空题提示补充「或难度档位」；node --check 通过 |
| T13 | `quiz.html` 取 tier 随 submit 回传 + 结果页档位标签 | `src/static/quiz.html` | ✅ 从 localStorage 读 `examTier` 随 `/api/exam/submit` 回传；结果 banner 显示「初级/进阶/挑战」；node --check 通过 |
| T14 | 历史记录/家长端展示 tier 标签 | `src/static/records.html`、`parent.html` | ✅ 学员端 records 与家长端 child records 列表/详情均显示档位标签（取 ExamRecordOut.tier，兜底初级）；node --check 通过 |

## P6 校验与部署

| ID | 任务 | 涉及文件 | 验收 |
|---|---|---|---|
| T15 | 本地 docker 容器全链路验证（迁移/组卷/积分/掌握度/学情） | 本地 `quiz-local` | ✅ 11 项校验 ALL PASS：迁移 0003_tier 启动自动应用；tier=1/2 组卷正确分流；mastery/me tiers 含档位；weakness/overview 按档位分流且 tier_name 正确；submit 积分 tier1=5/tier2=10（基础分×倍率） |
| T16 | ECS docker build 部署 + 验证 | ECS `quiz-system` | ✅ 部署前已 `/api/admin/backup-db` 备份；首轮只传 tier 改动文件导致 ECS `main.py` 滞后、`run_migrations` 未跑、新代码未加载——改为**全量同步 src 到 /app** 后重启，迁移 `0003_tier` 启动自动应用；修正迁移：因 `create_all` 预建空 `tier_config`，原 `if not table_exists` 守卫跳过了种子 INSERT，改为 `CREATE TABLE IF NOT EXISTS + INSERT OR IGNORE` 幂等播种；线上 `tier_config` 手动补种 3 行；验证：overview/student 学情 `tier_name` 初级/进阶/挑战均正确、`questions.tier` 已回填、数据卷保留 |

---

## 进度记录

- 2026-08-12 创建清单与设计方案。
- 2026-08-12 **T1–T3 完成**：迁移(0003_tier)、core/tier.py、models+schemas tier 贯通，容器内临时库验证迁移与倍率读取通过。
- 2026-08-12 **T4–T6 完成**：start_exam 三模式按 tier 抽题（tier=2 命中0/tier=1 全量）；submit 写 record.tier + 倍率积分实测 tier1=5/tier2=10/tier3=15 ALL PASS；questions 写入 tier 贯通。
- 2026-08-12 **T7–T9 完成**：mastery.py 作用域改 (uid,topic,tier) 算法零漂移；路由 me/student/class 按 tier 嵌套 + `?tier=`；前端 master.html 加档位 chip 切换、admin-mastery 加档位下拉并深链带 tier，node --check 通过。
- 2026-08-12 **T10 完成**：analytics.py 三接口(weakness/overview/student) 加 `tier` 过滤；`_student_mastery_map` 按档位计算；容器内直调验证 ALL PASS（档位正确分流 + tier_name）。
- 2026-08-12 **T11 完成**：admin.html 学情三个 pane 加「难度档位」下拉并传 tier；薄弱分析 hint 显示档位、跳转掌握度带 `&tier=`；node --check 通过。
- 2026-08-12 **T12 完成**：home.html 组卷/实操弹层加「难度档位」单选 chip（初级默认），请求带 tier 并写入 localStorage；node --check 通过。
- 2026-08-12 **T13 完成**：quiz.html 从 localStorage 读 tier 随提交回传，结果页 banner 显示档位标签；node --check 通过。
- 2026-08-12 **T14 完成**：records.html 与 parent.html 历史记录列表/详情显示档位标签（取 ExamRecordOut.tier）；node --check 通过。
- 2026-08-12 **T15 完成**：本地 docker 容器以 `TestClient(main.app)` 跑全链路 11 项校验 ALL PASS（迁移自动应用、组卷/学情按 tier 分流、积分倍率 tier1=5/tier2=10），临时验证脚本 `verify_t15.py` 已清理。
- 2026-08-12 **T16 完成**：ECS `quiz-system`(106.14.99.100:8000) 部署。部署前 backup-db 备份（quiz_20260812_123147_*.db）；首轮仅传 tier 改动文件致 ECS `main.py` 滞后、迁移未跑——改为全量同步 src→/app 后重启，迁移 `0003_tier` 启动自动应用（schema_migrations 落 `0003_tier`、`questions.tier` 全量回填 1）；修复迁移种子逻辑（`create_all` 预建空 `tier_config` 致原守卫跳过 INSERT，改 `INSERT OR IGNORE`）；线上补种 `tier_config` 3 行（倍率 1/2/3）；验证 overview/student 学情三档 `tier_name` 正确、数据卷保留。
