# REQ-4-1 验收报告

## 验收依据
- 需求文档：`docs/requirements/REQ-4.md`
- 设计文档：`docs/design/REQ-4-1.md`
- 任务清单：`docs/tasks/INDEX.md`（REQ-4-1-1/2/3 ✅，REQ-4-1-4 ✅）

## 验收结果

### 1. 代码改动（REQ-4-1-1 / REQ-4-1-2）✅
| 文件 | 改动 | 验证 |
|---|---|---|
| `src/models.py` | `name` 去掉 `unique=True`，加 `UniqueConstraint("name", "grade")` | ✅ |
| `src/migrations/0006_subject_grade_unique.py` | 幂等迁移：重建 subjects 表 + 加复合唯一 + Python 类 grade 置"通用" | ✅（容器启动自动执行） |
| `src/routers/subjects.py` | 校验逻辑改为 `(name, grade)` 组合查询 | ✅（端到端测试返回正确错误提示） |
| `src/schemas.py` | `SubjectOut` / `SubjectCreate` / `SubjectUpdate` 三处 `grade` 字段补中文注释 | ✅ |

### 2. 本地端到端验证（REQ-4-1-3）✅
测试脚本：`tests/api/test_subjects_req4.py`（跑在本地容器 `quiz-system`）

```
✅ Test 1 同名同年级 (数学/三年级) → 400 detail="同名同年级科目已存在"
✅ Test 2 同名跨年级 (数学/四年级) → 200 id=6, grade=四年级
✅ Test 3 全新科目 (英语/四年级、语文/四年级) → 200
✅ Test 4 Python 类科目 grade 应为'通用'（Python基础理论/实操 都=通用）
✅ Test 5 复合唯一约束：INSERT (数学/三年级) 第二次被 IntegrityError 拦住

汇总: 6/6 通过
```

### 3. ECS 拉库验证（REQ-4-1-4）✅
**操作**：从 ECS 备份下载最新数据库 → 替换本地库 → 重建本地镜像 + 容器 → 跑迁移 + 测试。

**ECS 数据真值**（验证前快照）：
- 5 个科目：Python基础理论(通用) / 数学(三年级) / Python基础实操(通用) / 语文(三年级) / 英语(三年级)
- `sqlite_autoindex_subjects_1` 单列索引（迁移 0006 在 ECS 也未跑过）
- `PRAGMA integrity_check = ok`

**本地验证结论**：
- 迁移 0006 在本地数据库上**自动跑通**（容器启动 `[migrate] schema up to date`）
- 复合唯一约束已生效（Test 5 业务逻辑验证）
- 同名跨年级新建功能可用

### 4. 部署交付要点（ECS 部署时必读）

**部署触发**：本地代码 + 测试已就绪，需按 `docs/deploy/ecs-deploy.md` 标准流程推 ECS。

**ECS 上待生效**：
1. **代码更新**（`models.py` / `subjects.py` / `schemas.py` / `0006 迁移`）—— 部署新镜像后生效
2. **迁移 0006 自动执行**——容器启动时会跑（幂等检查会跳过已迁移的库）
3. **业务功能解锁**——部署后可创建数学/四年级、英语/四年级、语文/四年级

**回滚预案**：
- 旧镜像 `quiz-system:latest` 保留（哈希 `3e4794859f5c`，部署前最后版本）
- 数据备份 `quiz_20260831_153522_343563.db`（ECS 触发备份，可下载回滚）
- 迁移 0006 是 RENAME TABLE 重建，理论可逆性差；如部署后出问题，从备份恢复是唯一可靠路径

### 5. 自定义基础镜像变更

新增 `docker/base/Dockerfile` + 镜像 `quiz-base:3.13`：
- 内嵌阿里云 pip 镜像源（避免每次构建重复配置）
- 时区固化 Asia/Shanghai
- quiz 项目 Dockerfile `FROM quiz-base:3.13`，不再写 pip 源

**注意**：ECS 上的 Dockerfile 也需要同步更新（部署时一并打包）。如不更新，ECS 仍会从 Docker Hub 拉 `python:3.13-slim`，构建速度慢但功能不受影响。

## 验收状态

| 项 | 状态 | 备注 |
|---|---|---|
| 复合唯一约束改造 | ✅ | 本地验证 6/6 通过 |
| 后端校验逻辑改造 | ✅ | 错误提示同步更新 |
| 数据迁移 0006 | ✅ | 幂等，本地已执行 |
| 设计文档同步 | ✅ | `docs/design/科目与知识点体系.md` 已更新 |
| ECS 拉库验证 | ✅ | 数据库替换 + 测试验证 |
| ECS 代码部署 | ⏳ 待执行 | 走 `/deploy` 流程 |

## 遗留事项

1. **ECS 部署**：本地验证完毕，需 `/deploy` 推 ECS 上线
2. **docker-base 镜像同步**：ECS 部署时若用相同 Dockerfile 需一并打包
3. **4 年级题库**：科目表已支持，**题目/课程待导入**（这是题库导入工作，不在本 REQ 范围）
4. **数据一致性**：本地 `data/quiz.db` 已被 ECS 库替换；旧本地库 `data/quiz_local_prereq4_bak.db` 保留留底

---
**完成时间**：2026-08-31
**验收人**：阿妮（Hermes） → 阿垚