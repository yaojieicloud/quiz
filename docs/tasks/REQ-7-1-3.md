# REQ-7-1-3 学员端：home.html 点击分流 + 文案改动

## 1. 任务描述

学员端首页 `home.html` 改动两处：
1. 理论科目点击卡片从"直接弹组卷"改为"跳 study.html"
2. 文案"选择题库"→"课程中心"，"开始今天的闯关"→"开始今天的学习"

## 2. 涉及文件

- `src/static/home.html`

## 3. 实施步骤

### 3.1 改文案（line 91-93）

```html
<div class="page-header">
  <div class="icon">📚</div>
  <h1>课程中心</h1>                                        <!-- 原：选择题库 -->
  <div class="subtitle">挑一个科目，开始今天的学习吧！</div>  <!-- 原：开始今天的闯关 -->
</div>
```

### 3.2 改 `startOrConfig`（line 337-344）

```javascript
async function startOrConfig(subjectId) {
  const sub = subjects.find(s => s.id === subjectId);
  if (isPractical(sub)) {
    openCoursePick(subjectId);   // 实操 → 现有选课弹层（不变）
  } else {
    // 理论 → 进新详情页（REQ-7-1）
    location.href = `study.html?subject_id=${subjectId}`;
  }
}
```

**实操保留原样**（确认是 `isPractical(sub)` 才走 `openCoursePick`），实操科目不受新流程影响。

### 3.3 标题 `<title>` 改（line 6）

```html
<title>课程中心 - 首页</title>  <!-- 原：题库闯关 - 首页 -->
```

## 4. 验证

- [ ] 首页标题显示"课程中心"
- [ ] 首页副标题显示"开始今天的学习吧！"
- [ ] 浏览器标签页 title 显示"课程中心 - 首页"
- [ ] 点击理论科目卡片（culture 类）→ 跳 study.html
- [ ] 点击实操科目卡片（programming 含"实操"）→ 仍弹原选课弹层

## 5. 状态

⏳待开发
