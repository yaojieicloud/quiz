# REQ-1-1-2 改写 analytics-shared.js 两个渲染函数

- 所属需求：REQ-1
- 所属设计：REQ-1-1
- 优先级：P0
- 依赖：REQ-1-1-1

## 任务内容
改写 `src/static/js/analytics-shared.js` 中 2 个函数为横向柱渲染：
- `renderMasteryCoverageChart(domId, d)`：保留「所选学员平均覆盖度」聚合，输出 `.hbar-list` HTML，含 80% 精通门槛线 `.hbar-masterline`
- `renderRecentTopicChart(domId, d)`：遍历 `d.by_topic`（前 20），输出同上结构，数值标签 `{answers}题 {rate}%`
- 移除这两图对 `echarts.init`/`chart.setOption`/`_sharedCharts` 的依赖，改为 `el.innerHTML`

## 验证
- node --check 语法通过；两个函数不再调用 echarts.init/setOption；`_sharedCharts` 保留供其他 ECharts 图使用。

## 状态
- ✅已完成（2026-08-25）
