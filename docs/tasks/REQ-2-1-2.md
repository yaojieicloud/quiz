# REQ-2-1-2: schemas.py 新增 schema

> 需求: REQ-2 答题后精通度变化展示
> 状态: ✅已完成

## 任务描述

在 `src/schemas.py` 中新增精通度变化的数据结构：

1. 新增 `MasteryDeltaOut` - 精通度变化输出模型
2. 在 `ExamRecordOut` 中新增 `mastery_deltas` 字段

## 实现要点

### MasteryDeltaOut 字段

```python
class MasteryDeltaOut(BaseModel):
    topic_name: str              # 课程名称
    before_pct: int              # 答题前精通度百分比（0-100）
    after_pct: int               # 答题后精通度百分比（0-100）
    delta: int                   # 变化量（after - before）
    newly_mastered: bool         # 是否新达到精通
    bottleneck: Optional[str]    # 瓶颈原因（rate/coverage/count/None）
```

### ExamRecordOut 修改

新增字段：
```python
mastery_deltas: List[MasteryDeltaOut] = []
```

## 验证

- Schema 定义正确 ✅
- Pydantic 验证通过 ✅

## 改动文件

- `src/schemas.py` - 新增 MasteryDeltaOut + 修改 ExamRecordOut
