# REQ-1-1-1 新增横向柱 CSS 样式（common.css）

- 所属需求：REQ-1
- 所属设计：REQ-1-1
- 优先级：P0
- 依赖：无

## 任务内容
在 `src/static/css/common.css` 新增横向进度条样式 + 响应式断点：
- `.hbar-list` / `.hbar-row` / `.hbar-label` / `.hbar-track` / `.hbar-fill` / `.hbar-masterline` / `.hbar-val` / `.hbar-emptytip`
- 窄屏断点 `@media (max-width:480px)`
- 入场动画 `hbar-grow`

## 验证
- 未涉及 JS 渲染，样式已追加到 common.css（3 页面共用），待 REQ-1-1-2 联调时验证。

## 状态
- ✅已完成（2026-08-25）
