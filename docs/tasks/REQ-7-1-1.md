# REQ-7-1-1 后端：迁移 + 模型 + schema 加字段

## 1. 任务描述

为 `topics` 表加 `tutorial_video_url`（B站视频）和 `tutorial_book_url`（在线课本）两个 TEXT 可空字段。涉及 `models.py`、`schemas.py`、新建迁移文件 `0010_topic_tutorial_urls.py`。

## 2. 涉及文件

- `src/models.py` — `Topic` 类加两列
- `src/schemas.py` — `TopicCreate`/`TopicUpdate`/`TopicOut` 各加两字段
- `src/migrations/0010_topic_tutorial_urls.py` — 新建，幂等加两列

## 3. 实施步骤

### 3.1 写迁移文件

`src/migrations/0010_topic_tutorial_urls.py`：
```python
"""REQ-7-1-1：topics 表加两列教程 URL 字段。

- tutorial_video_url: B站视频嵌入 URL（player.bilibili.com/...）
- tutorial_book_url:  在线课本 URL（人教版 book.pep.com.cn/...）
两列均 TEXT 可空，默认 NULL。
"""

MIGRATION_ID = "0010_topic_tutorial_urls"


def up(engine):
    from migrations import add_column
    add_column(engine, "topics", "tutorial_video_url", "TEXT", default=None)
    add_column(engine, "topics", "tutorial_book_url", "TEXT", default=None)
```

### 3.2 models.py 加列

`Topic` 类（line 79-97）末尾追加：
```python
tutorial_video_url = Column(Text, nullable=True)  # REQ-7 B站视频嵌入 URL
tutorial_book_url  = Column(Text, nullable=True)  # REQ-7 在线课本 URL
```

需要先确认 `from sqlalchemy import Column, Text, ...` 已导入（line 1-30 区域），若没有 Text 加上。

### 3.3 schemas.py 加字段

- `TopicOut`（line 76）：追加 `tutorial_video_url: Optional[str] = None` + `tutorial_book_url: Optional[str] = None`
- `TopicCreate`（line 107）：追加同两字段
- `TopicUpdate`（line 113）：追加同两字段

`Optional` 已 import 过（line 1 区域），复用即可。

## 4. 验证

- [ ] 迁移文件能被自动加载（启动时跑 `run_migrations`）
- [ ] 迁移幂等：第二次启动不报错
- [ ] `data/quiz.db` 启动后 `PRAGMA table_info(topics)` 含新两列
- [ ] `GET /api/subjects/{id}/topics` 返回新字段
- [ ] `PUT /api/topics/{id}` 带新字段保存后能 GET 出来

## 5. 状态

⏳待开发
