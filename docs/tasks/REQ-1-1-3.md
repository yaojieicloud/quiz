# REQ-1-1-3 三页面容器适配 + 文档同步 + 联调验证

- 所属需求：REQ-1
- 所属设计：REQ-1-1
- 优先级：P1
- 依赖：REQ-1-1-2

## 任务内容
- `src/static/admin.html`、`src/static/mastery.html`、`src/static/stats.html`：`#chartMasteryDist`/`#chartRecentTopic` 的 `height:320px` 改为由 CSS 控制（去掉内联固定高度，交给 `.chart-box`）
- 文档同步：`docs/design/管理后台与学情分析.md`、`docs/design/掌握度与学情联动设计.md` 前端清单章节，标注两柱状图改为横向响应式
- 联调验证：admin 掌握度全景 + 最近动态、mastery 掌握度、stats 最近动态 4 个实例逐一验证（含 <480px 窄屏），确认其余 ECharts 图不受影响

## 验证
- 4 个横向柱容器加 `hbar-box`，去除内联固定高度；common.css 增 `.hbar-box`（自适应+滚动）。
- headless Chrome 渲染验证页：6 行横向柱生成正确（label/fill宽/颜色/数值均正确）；400px 窄屏仍 6 行不重叠。
- 接口 `admin/mastery`、`recent-activity` 均 200，数据结构与渲染函数匹配。
- 临时验证文件已清理。

## 状态
- ✅已完成（2026-08-25）
